# Excel database snapshots

This directory contains human-readable Excel snapshots generated from the canonical JSON data under `packs/`.

> Do not edit `.xlsx` files manually. Update the source JSON in `packs/`; GitHub Actions regenerates the corresponding versioned Excel snapshot automatically.

Current database links are updated by `scripts/generate_excel.py`.
